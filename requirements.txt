# Flask web framework
Flask==2.3.4

# Pillow (image processing) — use stable prebuilt version
Pillow==10.0.1

# Firebase admin SDK
firebase-admin==6.8.0

# Requests for HTTP calls
requests==2.32.4

# Optional: Google API dependencies (required by firebase-admin)
google-api-core[grpc]==2.28.0
google-auth==2.42.1
cachetools==6.2.1
charset-normalizer==3.4.3

# Werkzeug dependency for Flask
Werkzeug==2.3.7
